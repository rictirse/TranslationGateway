<UserControl x:Class="Translation.Gateway.Views.KnowledgeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Translation.Gateway.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance vm:KnowledgeViewModel, IsDesignTimeCreatable=True}"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="10">
        <TabControl>
            <TabItem Header="翻譯快取管理 (SQLite)">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="搜尋原文：" VerticalAlignment="Center"/>
                        <TextBox Width="200" Margin="5,0" Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"/>
                        <Button Content="重新整理" Command="{Binding LoadDataCommand}" Width="80" Margin="5,0"/>
                    </StackPanel>

                    <DataGrid Grid.Row="1" ItemsSource="{Binding CacheItems}" 
                              SelectedItem="{Binding SelectedCacheItem}"
                              AutoGenerateColumns="False" IsReadOnly="True" 
                              SelectionMode="Single">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="原文" Binding="{Binding SourceText}" Width="1*"/>
                            <DataGridTextColumn Header="譯文" Binding="{Binding TargetText}" Width="1*"/>
                            <DataGridCheckBoxColumn Header="人工校正" Binding="{Binding IsManual}" Width="Auto"/>
                            <DataGridTextColumn Header="最後使用" Binding="{Binding LastUsed, StringFormat={}{0:MM/dd HH:mm}}" Width="Auto"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <GroupBox Grid.Row="2" Header="手動校正內容" Margin="0,10,0,0" Padding="5">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="修改譯文："/>
                                <TextBox Text="{Binding EditingTargetText, UpdateSourceTrigger=PropertyChanged}" 
                                         Margin="0,5" Height="30" VerticalContentAlignment="Center"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" VerticalAlignment="Bottom" Margin="10,0,0,5">
                                <Button Content="儲存校正" Command="{Binding SaveCorrectionCommand}" Width="100" Height="30" Background="#28a745" Foreground="White"/>
                            </StackPanel>
                        </Grid>
                    </GroupBox>
                </Grid>
            </TabItem>

            <TabItem Header="角色設定 (MCP)">
                <Grid Margin="10">
                    <TextBlock Text="MCP 角色卡編輯功能開發中..." HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16" Foreground="Gray"/>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>